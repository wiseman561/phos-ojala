import React from 'react';
import { Box, Typography } from '@mui/material';
import theme from '../theme';

/**
 * AlertsScreen Component (Web Stub)
 *
 * Minimal web-compatible version of the AlertsScreen component
 */
const AlertsScreen = ({ navigation }) => {
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="h4" gutterBottom>
        Alerts Screen
      </Typography>
      <Typography variant="body1">
        This is a placeholder for the alerts screen functionality.
      </Typography>
    </Box>
  );
};

export default AlertsScreen;
