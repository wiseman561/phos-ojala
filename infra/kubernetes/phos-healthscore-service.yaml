apiVersion: v1
kind: Service
metadata:
  name: phos-healthscore
  namespace: demo
  labels:
    app: phos-healthscore
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: phos-healthscore 