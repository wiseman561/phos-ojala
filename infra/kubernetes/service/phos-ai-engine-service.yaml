apiVersion: v1
kind: Service
metadata:
  name: phos-ai-engine
  namespace: demo
  labels:
    app: phos-ai-engine
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: phos-ai-engine
