apiVersion: v1
kind: Service
metadata:
  name: phos-apigateway
  namespace: demo
  labels:
    app: phos-apigateway
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: phos-apigateway
