apiVersion: v1
kind: Service
metadata:
  name: phos-nurse-assistant
  namespace: demo
  labels:
    app: phos-nurse-assistant
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: phos-nurse-assistant
