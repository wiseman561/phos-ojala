apiVersion: v1
kind: Service
metadata:
  name: phos-patientportal
  namespace: demo
  labels:
    app: phos-patientportal
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: phos-patientportal
