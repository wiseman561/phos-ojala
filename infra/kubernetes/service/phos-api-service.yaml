apiVersion: v1
kind: Service
metadata:
  name: phos-api
  namespace: demo
  labels:
    app: phos-api
    component: backend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: phos-api
