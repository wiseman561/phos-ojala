apiVersion: v1
kind: Service
metadata:
  name: phos-employer-dashboard
  namespace: demo
  labels:
    app: phos-employer-dashboard
    component: frontend
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: phos-employer-dashboard
