apiVersion: apps/v1
kind: Deployment
metadata:
  name: phos-patient-app
  namespace: demo
  labels:
    app: phos-patient-app
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: phos-patient-app
  template:
    metadata:
      labels:
        app: phos-patient-app
        component: frontend
    spec:
      containers:
      - name: phos-patient-app
        image: phos/patient-app:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        resources:
          limits:
            cpu: "300m"
            memory: "256Mi"
          requests:
            cpu: "100m"
            memory: "128Mi"
        env:
        - name: NODE_ENV
          value: "production"
        - name: API_URL
          value: "http://phos-apigateway.demo.svc.cluster.local"
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 15
          periodSeconds: 5
