apiVersion: apps/v1
kind: Deployment
metadata:
  name: phos-web
  namespace: demo
  labels:
    app: phos-web
    component: frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: phos-web
  template:
    metadata:
      labels:
        app: phos-web
    spec:
      containers:
        - name: phos-web
          image: yourdockerhubuser/phos-web:latest   # ← update with your registry & tag
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80   # Nginx listens on 80 in your Dockerfile
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
