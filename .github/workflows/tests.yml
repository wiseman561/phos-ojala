name: Build and Test (legacy - disabled)

on:
  workflow_dispatch:
    inputs:
      note:
        description: 'Legacy workflow disabled in favor of phos-ci.yml'
        required: false

env:
  DOTNET_VERSION: '9.0.203'
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs: {}
