# Database
DB_HOST=phos-db
DB_PORT=5432
DB_NAME=phos
DB_USER=phos_user
DB_PASSWORD=SuperSecret123

# JWT (keep these secret)
JWT_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9…
JWT_ISSUER=phos-issuer
JWT_AUDIENCE=https://phos-api

# HealthScore connection
HEALTHSCORE_DB_CONN=Host=phos-db;Port=5432;Database=healthscore;Username=${DB_USER};Password=${DB_PASSWORD}

# AI model path
AI_MODEL_PATH=/app/models/healthscore_model.onnx

# Full API connection string
DB_CONNECTION_STRING=Host=phos-db;Port=5432;Database=${DB_NAME};Username=${DB_USER};Password=${DB_PASSWORD}
