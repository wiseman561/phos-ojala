apiVersion: v1
kind: Service
metadata:
  name: phos-core
spec:
  type: {{ .Values.service.type }}
  selector:
    app: phos-core
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
