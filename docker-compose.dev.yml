version: '3.8'

services:
  # Redis for event bus
  phos-redis:
    image: redis:7-alpine
    container_name: phos-redis-dev
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - phos-dev-network

  # PostgreSQL database
  phos-db:
    image: postgres:15-alpine
    container_name: phos-db-dev
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=phos
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - phos-dev-network

volumes:
  redis-data:
  postgres-data:

networks:
  phos-dev-network:
    driver: bridge
